<html>
<head><title>{{ title }}</title></head>
<body>
    {% include '_header.html.twig' with {'siteName': siteName} %}
    <main>
        <h2>{{ heading }}</h2>
        <p>{{ content }}</p>
    </main>
</body>
</html>
